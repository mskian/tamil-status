module.exports = {
    siteUrl: `https://hello.tamilimages.xyz`, // Site domain. Do not include a trailing slash!

    postsPerPage: 12, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `Tamil Status`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `Tamil SMS, Kavithai and Quotes.`, // This allows an alternative site description for meta data for pages.

    shareImage: `/images/tml-status-thumb.png`, // fallback image for meta data. Will be used, when no post/tag/author image specified. 1200x1200 is recommended
    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `Tamil Status`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#e9e9e9`, // Used for Offline Manifest
    themeColor: `#15171A`, // Used for Offline Manifest
}
